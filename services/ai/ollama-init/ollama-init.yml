ollama-init:
  build: .
  container_name: ollama-init
  depends_on:
    - ollama
  environment:
    - OLLAMA_MODELS=llama3 llava phi3
    - OLLAMA_HOST=ollama:11434
  command: >
    sh -c "sleep 5 && echo $$OLLAMA_MODELS | xargs -n 1 ollama pull"
