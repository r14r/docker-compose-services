radarr:
  image: linuxserver/radarr:latest
  container_name: radarr
  restart: unless-stopped
  ports:
    - "7878:7878"
  environment:
    - PUID=1000
    - PGID=1000
    - TZ=UTC
  volumes:
    - radarr_config:/config
    - ./media/movies:/movies
    - ./downloads:/downloads
