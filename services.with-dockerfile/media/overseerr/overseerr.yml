overseerr:
  build: .
  container_name: overseerr
  restart: unless-stopped
  ports:
    - "5055:5055"
  environment:
    - LOG_LEVEL=debug
    - TZ=UTC
    - PORT=5055
  volumes:
    - overseerr_config:/app/config
