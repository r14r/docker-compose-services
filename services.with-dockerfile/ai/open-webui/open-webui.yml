open-webui:
  build: .
  container_name: open-webui
  restart: unless-stopped
  ports:
    - "8081:8080"  # adjust host port to avoid conflicts with other services
  volumes:
    - open_webui_data:/app/backend/data
  environment:
    - OLLAMA_BASE_URL=http://ollama:11434
  depends_on:
    - ollama
